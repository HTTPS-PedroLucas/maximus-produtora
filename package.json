{
  "name": "maximus-produtora",
  "version": "1.0.0",
  "private": true,
  "description": "Sistema de planejamento de captações da Máximus Produtora (API + interface)",
  "engines": {
    "node": ">=22.5.0"
  },
  "scripts": {
    "install:all": "npm --prefix backend install && npm --prefix frontend install",
    "build": "npm --prefix frontend install && npm --prefix frontend run build && npm --prefix backend install --omit=dev",
    "start": "node backend/src/server.js",
    "migrate": "npm --prefix backend run migrate",
    "seed": "npm --prefix backend run seed",
    "seed:demo": "npm --prefix backend run seed:demo",
    "backup": "npm --prefix backend run backup",
    "test": "npm --prefix backend test",
    "lint": "npm --prefix frontend run lint"
  }
}
